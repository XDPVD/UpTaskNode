<!DOCTYPE html>
html(lang="en")
    head
       include partials/header 
    body.login
        .contenedor-formulario 
            h1 Iniciar Sesión en UpTask
            
            //pre= vardump(locals)
            //pre= vardump(mensajes)
            if locals.mensajes
                .alertas
                    each categoria in Object.keys(locals.mensajes)
                        each error in mensajes[categoria]
                            .alerta(class=`${categoria}`)
                                p= error

            form#formulario.caja.caja-login(method="POST" action="/iniciar-sesion")
                .campo
                    label(for="email") E-mail:
                    input(type="email" name="email" placeholder="Tu email")
                .campo
                    label(for="password") Password:
                    input(type="password" name="password" placeholder="password")
                .campo.enviar
                    input.boton(type="submit" value="Iniciar sesión" )
                .campo.acciones
                    a(href="/crear-cuenta") ¿Aún no tienes cuenta? ¡Crea gratis una!
                    a(href="/reestablecer") ¿Olvidates tu contraseña?
        script(src="/dist/bundle.js")
        